// uz++ — Compile-time modifiers test (Stage 1)
// sobit_ifoda       -> constexpr
// sobit_baholash    -> consteval
// sobit_boshlangich -> constinit

ulash "uzpp_runtime.hpp"

// constexpr function
sobit_ifoda butun kvadrat(butun x) {
    qaytarish x * x;
}

// consteval function — MUST be evaluated at compile time
sobit_baholash butun kub(butun x) {
    qaytarish x * x * x;
}

// constinit global — guaranteed initialised at program start
sobit_boshlangich butun global_son = 42;

// constexpr global
sobit_ifoda butun PI_TAQRIBAN = 3;

butun asosiy() {
    // Compile-time constant
    sobit_ifoda butun a = kvadrat(5);
    sobit_ifoda butun b = kub(3);

    yozish << "kvadrat(5) = " << a << qator_oxiri;
    yozish << "kub(3) = " << b << qator_oxiri;
    yozish << "global_son = " << global_son << qator_oxiri;
    yozish << "PI_TAQRIBAN = " << PI_TAQRIBAN << qator_oxiri;

    yozish << "=== MUVAFFAQIYATLI ===" << qator_oxiri;
    qaytarish 0;
}
